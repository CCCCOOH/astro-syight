---
const { name, link, color, dark_color:darkColor } = Astro.props;
---

<a class="text-white! rounded shadow" href={link}>{name}</a>

<style define:vars={{color, darkColor}}>
a {
  padding: 5px 10px;
  background-color: var(--color);
}
:global(.dark) a {
  background-color: var(--darkColor);
}
</style>